var assert = require('assert')
var LinkedList = require('../dist/LinkedList')

describe('Linked list', function() {
  var llist

  beforeEach(function() {

  })

  it('should have correct size', function() {

  })

  it('should have null as the last node', function() {

  })

  it('should', function() {

  })
})
