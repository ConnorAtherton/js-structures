//
// 1]
//
// Given an array of numbers, return the longest substring in
// which the difference between every consecutive number pair
// varies difference in sign (I.e. + - + -).
//
const substringSigns = (str) => {

}

//
// 2]
//
// Given a zero-indexed array A of N integers, return any of the indexes P of the element, which
// left sums of its left elements and right elements are equal. Sum of zero element is assumed to be
// equal to 0. This can happen if P = 0 or if P = N-1.
//
// For example, A = {-1, 3, -4, 5, 1, -6, 2, 1}, indexes 1, 3, and 7 are valid outputs.
//
const arrayElementSums = (arr) => {

}

export default {
  substringSigns
}

