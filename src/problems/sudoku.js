//
// 1]
//
// Write a function that determines whether a standard sudoku board is valid
//
// Board is represented by a NxN matrix.
//
const valid = (board) => {
  // const squaresValid = board.squares.any(square => )
  // const rowsValid = board.rows.any(row => )
  // const columnsValid = board.columns.any(column => )

  // return squaresValid && rowsValid && columnsValid;
}
